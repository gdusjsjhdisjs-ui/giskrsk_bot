# 📋 Аудит v14 — проверка исправлений

## ✅ Что пофикшено (12 из 25 багов)

| # | Баг | Статус | Детали |
|---|-----|--------|--------|
| 1 | `worker_entry.py` отсутствует | ✅ ФИКС | Файл создан, worker запускается |
| 2 | `arq` нет в requirements | ✅ ФИКС | `arq>=0.26.0` добавлен (был в v13) |
| 4 | `aiohttp-socks` не установлен | ✅ ФИКС | Добавлен в зависимости (был в v13) |
| 5 | Scheduler закомментирован | ✅ ФИКС | Теперь запускается в `main.py` |
| 10 | `GeoServiceClient` undefined | ✅ ФИКС | Импорт исправлен |
| 11 | Dockerfile без worker | ✅ ЧАСТИЧНО | Добавлен комментарий, но `CMD` всё ещё один |
| 16 | Redis всегда обязателен | ✅ ФИКС | `_MemoryStore` — фолбэк в память |
| 19 | Стиль карты ужасный | ✅ ФИКС | Градиенты, glassmorphism, анимации, тёмная тема |
| 20 | Нет функционала на карте | ✅ ЧАСТИЧНО | Линейка (📏) добавлена |
| 21 | Нет кнопок назад/отмена | ✅ ФИКС | `/cancel`, `/help`, меню команд |
| 22 | Нет `/cancel` для FSM | ✅ ФИКС | `cmd_cancel` с `state.clear()` |
| 25 | AccountPool пересоздаётся | ✅ ФИКС | Теперь синглтон `_get_pool()` |

---

## ❌ Что НЕ ПОЧИНЕНО (13 багов осталось)

### Критические (🔴)

| # | Баг | Почему не починено |
|---|-----|-------------------|
| 6 | **Batch-обработка синхронная** — `process_job` вызывается напрямую в хендлере, не через ARQ-очередь | Блокирует бот при загрузке CSV |
| 7 | **`WEBAPP_URL` пустой** — карта не открывается в Telegram | `.env` не заполнен |
| 13 | **Реальные ПЗЗ не развёрнуты** — в `webapp/data/` только `demo.geojson` | Данные из архива не скопированы |
| 18 | **demo.geojson — 6 прямоугольников** — без формы, без привязки к реальности | Не заменён на реальные ПЗЗ |

### Важные (🟡)

| # | Баг | Почему не починено |
|---|-----|-------------------|
| 8 | **YooKassa placeholder** — оплата не работает | Нужны реальные ключи от ЮKassa |
| 9 | **NextGIS bearer_token = placeholder** — поиск участков мёртв | Нужен реальный токен NextGIS |
| 12 | **`start.bat` не ищет Python в `AppData`** | Сломан на Win без PATH |
| 15 | **Нет Menu Button в BotFather** | Ручная настройка в @BotFather |
| 17 | **Бэкап — .env с токенами в Telegram** | Риск утечки, формат архива не описан |
| 20 | **Нет: измерение площади, рисование, экспорт, фильтр зон, координаты, масштаб** | Из всего функционала добавлена только линейка |
| 24 | **TelegramClient создаёт второй Bot** | В worker — лишнее соединение с Telegram |

### Незначительные (⚠️)

| # | Баг | Почему не починено |
|---|-----|-------------------|
| 23 | **Напоминания — возможны дубликаты** | Гонки после рестарта не исправлены |

---

## 📊 ИТОГО

| Статус | Кол-во |
|--------|--------|
| ✅ Полностью починено | 10 |
| ✅ Частично починено | 2 |
| ❌ Осталось критических | 4 |
| ❌ Осталось важных | 7 |
| ❌ Осталось незначительных | 1 |

### Что в первую очередь доделывать:
1. **Развернуть реальные ПЗЗ** — скопировать `pzz_*.geojson` из архива в `webapp/data/` и подключить в `CONFIG.layers`
2. **Заполнить `.env`** — `WEBAPP_URL`, `NEXTGIS_BEARER_TOKEN`, `YOOKASSA_*`
3. **Перенести batch-обработку в ARQ** — через `arq.enqueue_job`
4. **Убрать `.env` из бэкапа** — `backup_service.py`, строка `DATA_FILES`
